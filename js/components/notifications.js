export function renderNotifications(notifications = []) { return notifications.filter(item => !item.read).map(item => `<div class="notification-item"><p>${item.text}</p>${item.action === 'credit-payment' ? `<button class="table-action" data-action="notification-credit" data-credit-id="${item.targetId}">Registrar abono</button>` : item.action === 'payable-payment' ? `<a class="table-action" href="#cuentas-por-pagar">Ver cuenta por pagar</a>` : item.action === 'inventory' ? `<a class="table-action" href="#inventario">Ver inventario</a>` : ''}</div>`).join('') || '<p>No hay notificaciones pendientes.</p>'; }
