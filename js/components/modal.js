export function closeModal() { document.querySelector('#modal-root').innerHTML = ''; }
export function showModal({ title, label, onSubmit }) { document.querySelector('#modal-root').innerHTML = `<div class="modal-backdrop"><form class="modal"><button type="button" class="modal-close">×</button><p class="eyebrow">FAMIMUEBLES ERP</p><h2>${title}</h2><label class="input-label">${label}</label><input class="field" required autofocus placeholder="Escribe aqui..."><button class="primary wide">Guardar</button></form></div>`; document.querySelector('.modal').addEventListener('submit', event => { event.preventDefault(); onSubmit(event.target.querySelector('input').value); closeModal(); }); document.querySelector('.modal-close').addEventListener('click', closeModal); }
