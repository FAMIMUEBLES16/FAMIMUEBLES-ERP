import { page } from '../components/tables.js';
export function renderReportes(data = {}) { const counts=[['ventas','sales'],['inventario','inventoryByStore'],['cartera','credits'],['clientes','customers'],['locales','stores'],['creditos','credits'],['gastos','expenses'],['compras','purchases']]; const stores=(data.stores||[]).map(item=>`<option value="${item.id}">${item.name}</option>`).join(''); return page('ANALITICA','Reportes','',`<section class="panel report-filters"><div class="panel-head"><h3>Filtros</h3><span class="muted">Periodo y local</span></div><div class="filter-row report-filter-row"><label class="input-label">Desde<input class="field" id="report-from" type="date"></label><label class="input-label">Hasta<input class="field" id="report-to" type="date"></label><label class="input-label">Local<select class="field" id="report-store"><option value="">Todos</option>${stores}</select></label></div></section><div class="report-grid">${counts.map((item,index)=>`<article class="report-card"><span class="report-icon">${['↗','▦','◌','♙','⌂','◫','$','▤'][index]}</span><h3>Reporte de ${item[0]}</h3><strong>${data[item[1]]?.length||0}</strong><p>Registros disponibles</p><button class="outline" data-action="report" data-report-collection="${item[1]}">Descargar PDF</button></article>`).join('')}</div>`); }
