import { page, table, badge } from '../components/tables.js';
export function renderUsuarios(state) { return page('ADMINISTRACION','Usuarios y permisos', '<button class="primary" data-action="new-user">＋ Nuevo usuario</button>', `<div class="settings-grid"><section class="panel"><h3>Usuarios</h3>${table(['Usuario','Correo','Telefono','Rol','Estado','Acciones'],state.users.map(user=>`<tr><td><strong>${user.name || user.username}</strong></td><td>${user.email || '-'}</td><td>${user.phone || '-'}</td><td>${user.role}</td><td>${badge(user.status)}</td><td><button class="table-action" data-action="edit-user" data-user-id="${user.id}">Editar</button><button class="table-action" data-action="user-permissions" data-user-id="${user.id}">Permisos</button></td></tr>`))}</section><section class="panel"><h3>Permisos granulares</h3><p class="muted">Selecciona Permisos junto a un usuario para configurar sus accesos.</p></section></div>`); }
